---
layout: main
title: Post Archives
pagination:
    data: collections.posts
    size: 5
    alias: posts
    reverse: true
---
<h2>Post Archives</h2>
<ui>
    {% for post in posts %}
        <li>
            <a href="{{ post.url }}">{{ post.data.title }}</a><i>{{ post.date | date: "%d-%m-%y" }}</i>
        </li>
    {% endfor %}
</ui>
<p>
    {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}">Previous</a>
    {% else %}
    Previous
    {% endif %} /
    {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">Next</a>
        {% else %}
        Next
    {% endif %}
</p>

<p>
    Page {{ pagination.pageNumber | plus:1 }} of {{ pagination.pages.length }}
</p>
